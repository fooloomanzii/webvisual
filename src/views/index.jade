extends layout

block content

  post-card#p3(headline="Test-Postcard")
    value-watcher()
    include inhalt

  value-collection-demo()
  //- music-demo()
  //- value-sign-collection()
    //- include inhalt

  script.
    document.addEventListener("dataLoaded", function(event){
      console.log("dataLoaded ready");
      $("value-sign-collection").attr("receive", JSON.stringify(event.detail));
      $("value-collection-demo").attr("receive", JSON.stringify(event.detail));

    });
